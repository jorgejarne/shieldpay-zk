{
  "name": "@shieldpay/demo-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/server.ts",
  "types": "src/server.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/server.ts"
  },
  "dependencies": {
    "@shieldpay/merchant-api": "0.1.0",
    "@shieldpay/shared-types": "0.1.0",
    "express": "^4.21.2"
  }
}
